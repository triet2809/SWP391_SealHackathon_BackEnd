
server:
  port: 8080

spring:
  application:
    name: seal-hackathon-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/SEAL_HACKATHON?stringtype=unspecified
    username: postgres
    password: 123456
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: false

  security:
    user:
      name: ec.seal@fpt.edu.vn
      password: test123